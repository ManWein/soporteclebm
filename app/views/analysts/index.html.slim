h1
  | Analysts
  = link_to "(New)", new_analyst_path

table.table.table-hover
  thead
    tr
      th# Email
      th# Name
      th# Username
      th#
  tbody
    - if @analysts.blank?
      tr
        - (0..3).each do |a|
          td -
    - else
      - @analysts.each do |a|
        tr
          td = a.email
          td = a.name
          td = a.username
          td.vanity-actions
            = link_to "Edit", edit_analyst_path(a)
            = link_to "Delete", analyst_path(a), {data: {confirm: 'Are you sure?'}, method: :delete}

= link_to "Back", dashboard_manager_path(current_user)